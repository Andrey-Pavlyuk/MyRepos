import React from "react";
import styles from "./LikesCounter.module.scss";

const LikesCounter = () => {
    return (
        <span className={styles.likesCounter}>315</span>
    );
};

export default LikesCounter